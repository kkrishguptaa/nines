{
  "name": "nines",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "python -m pip install -U pip setuptools wheel && if [ -f pyproject.toml ]; then python -m pip install -e \".[dev]\"; else python -m pip install pytest pytest-asyncio hypothesis anthropic httpx hatchling; fi && python --version && pytest --version && gh --version"
}
