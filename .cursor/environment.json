{
  "name": "nines",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "python3 -m pip install -U pip setuptools wheel && if [ -f pyproject.toml ]; then python3 -m pip install -e \".[dev]\"; else python3 -m pip install pytest pytest-asyncio hypothesis anthropic httpx hatchling; fi && python3 --version && gh --version"
}
